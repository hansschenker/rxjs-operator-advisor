{
  "name": "rxjs-operator-advisor",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Describe the behavior you want, get the right RxJS higher-order operator. A standalone advisor that reuses the RxJS ontology (property-graph JSON) as its knowledge source.",
  "license": "MIT",
  "bin": {
    "rxjs-advise": "src/cli.ts"
  },
  "scripts": {
    "build": "tsc --noEmit",
    "advise": "tsx src/cli.ts",
    "test": "vitest run"
  },
  "devDependencies": {
    "@types/node": "^22.10.2",
    "tsx": "^4.19.2",
    "typescript": "^5.7.2",
    "vitest": "^2.1.8"
  }
}
